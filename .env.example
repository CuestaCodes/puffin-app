# Puffin Environment Variables
# Copy this file to .env.local and fill in your values

# =============================================================================
# SESSION SECURITY (Required for production)
# =============================================================================

# Secret key for session encryption (generate with: openssl rand -base64 32)
SESSION_SECRET=your-secret-key-here-min-32-chars

# Session cookie name (optional, defaults to 'puffin_session')
# SESSION_COOKIE_NAME=puffin_session

# =============================================================================
# GOOGLE OAUTH (Optional - for Google Drive sync)
# =============================================================================
# These can also be entered via the UI wizard in Settings > Sync

# Google Cloud OAuth 2.0 Client ID
# GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com

# Google Cloud OAuth 2.0 Client Secret
# GOOGLE_CLIENT_SECRET=your-client-secret

# Google API Key (optional, for additional API access)
# GOOGLE_API_KEY=your-api-key

# OAuth redirect URI (defaults to http://localhost:3000/api/sync/oauth/callback)
# GOOGLE_REDIRECT_URI=http://localhost:3000/api/sync/oauth/callback

# =============================================================================
# ENCRYPTION (Optional)
# =============================================================================

# Custom encryption key for OAuth tokens and credentials
# If not set, a machine-derived key is generated from hostname + username
# SYNC_ENCRYPTION_KEY=your-encryption-key

# =============================================================================
# DATABASE (Optional)
# =============================================================================

# Custom data directory (defaults to ./data in development)
# PUFFIN_DATA_DIR=/path/to/data

# =============================================================================
# SECURITY TUNING (Optional)
# =============================================================================

# bcrypt rounds for PIN hashing (default: 12, higher = slower but more secure)
# BCRYPT_ROUNDS=12
